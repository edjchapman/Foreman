# Auto-provisioned datasource for the observability profile (`make observe`).
apiVersion: 1

datasources:
  - name: Prometheus
    uid: foreman-prom
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
