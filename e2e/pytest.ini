# Standalone config for the e2e suite: the root pyproject pytest config is
# unit-suite-specific (django settings, --reuse-db, filterwarnings=error) and
# must not apply here. Run via `make e2e` (pytest -c e2e/pytest.ini e2e).
[pytest]
addopts = -q --browser chromium
